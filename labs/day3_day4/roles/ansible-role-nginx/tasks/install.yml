---
- name: Add epel-release repo
  yum:
    name: epel-release
    state: present 
    
- name: Install nginx
  yum:
    name: nginx
    state: present # present là cài đặt 
  notify: start nginx # dùng notify thì dùng handler gọi ra




